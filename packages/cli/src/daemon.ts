#!/usr/bin/env node
// `hotcelld` shipped by the flagship `hotcell` package: starts the bundled
// daemon (same entrypoint as @hotcell/daemon's own bin), so a single
// `npm install -g hotcell` provides both the CLI and the daemon.
import "@hotcell/daemon";
